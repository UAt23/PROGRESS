apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "californiaweb.fullname" . }}-test-connection"
  labels:
    {{- include "californiaweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "californiaweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
