apiVersion: v1
kind: Service
metadata:
  name: {{ include "californiaweb.fullname" . }}
  labels:
    {{- include "californiaweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "californiaweb.selectorLabels" . | nindent 4 }}
